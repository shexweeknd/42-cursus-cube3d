#include "cube.h"

int main(void)
{
    // void *mlx;
    // void *win;

    // mlx = mlx_init();
    // win = mlx_new_window(mlx, 500, 500, "cube");
    
    // mlx_loop(mlx);

    // mlx_destroy_window(mlx, win);
    // mlx_destroy_display(mlx);
    ft_putstr("Hello, World!\n");
    return (0);
}